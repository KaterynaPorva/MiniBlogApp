@page
@model ErrorModel
@{
    Layout = "_Layout";
    ViewData["Title"] = "Помилка";
}

<style>
    body {
        background-color: #5C4033; 
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        width: 100%;
    }

    .error-container {
        max-width: 800px;
        margin: 100px auto;
        padding: 30px;
        background-color: #ffffff;
        border-radius: 20px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.2);
        color: #333;
    }

    h1, h2, h3 {
        text-align: center;
        color: #d32f2f; 
        margin-bottom: 20px;
    }

    p {
        margin-bottom: 15px;
        text-align: center;
    }

    code {
        background-color: #f5f5dc;
        padding: 2px 5px;
        border-radius: 5px;
    }

    .links {
        text-align: center;
        margin-top: 20px;
    }

        .links a {
            color: #5C4033;
            text-decoration: none;
            font-weight: bold;
            margin: 0 10px;
        }

            .links a:hover {
                text-decoration: underline;
            }
</style>

<div class="error-container">
    <h1>Помилка</h1>
    <h2>Сталася помилка під час обробки запиту.</h2>

    @if (Model.ShowRequestId)
    {
        <p>
            <strong>Request ID:</strong> <code>@Model.RequestId</code>
        </p>
    }

    <h3>Режим розробника</h3>
    <p>
        Якщо ввімкнено <strong>Development</strong> середовище, відображається детальна інформація про помилку.
    </p>
    <p>
        <strong>Не слід вмикати Development на продакшн-сервері.</strong>
        Це може призвести до показу чутливої інформації кінцевим користувачам.
    </p>

    <div class="links">
        <a href="/Index">⬅ На головну</a>
        <a href="/MyPosts">Мої пости</a>
    </div>
</div>
